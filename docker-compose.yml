version: '3.8'
services:
  app:
    build: .
    ports:
      - "5001:5000"
    environment:
      - DB_HOST=your_db_host
      - DB_USER=your_db_user
      - JWT_SECRET=${JWT_SECRET}
    restart: always